---
title: 学习与删除候选
order: 6
description: 青简在本机如何记录输入习惯、自动造词、删除不需要的候选，以及「统计」页的内容。
---

## 学习方式

- 选过的词、同一段拼音下选过的词、连续输入的词序列，均在本机记录，整句转换与排序据此调整。
- 连续选出的两个词合起来不在词库中且选择次数达到阈值时，自动记为一个词。
- 全部在本机，不上传。文件位置与清除方式见 [数据与日志](../help/data-and-logs.md)。

## 删除不需要的候选

输入拼音时按 `⇧ + 数字`（Windows：`Shift + 数字`）删除当前页第 N 个候选：
用户自造的词、云端选过的词整个删除；词库中的词不删除，但清除对它的全部学习记录，恢复原始排序。
删除后拼音行下方短暂显示结果（「已删除用户词「…」」，或词库词无学习记录时的「没什么可删」），输入下一个键即消失。

修饰键在「偏好设置 → 快捷键」（Windows：「设置 → 快捷键」）录制修改。
macOS 上应避开 `⌃ + 数字`（系统切换桌面）与 `⌘ + 数字`（应用切换标签页）；Windows 上 `Alt + 数字` 会被应用作为菜单快捷键截获。
全部按键见 [按键与快捷键](../getting-started/keys.md)。

## 统计

「偏好设置 → 统计」显示今天 / 最近 7 天 / 累计输入的汉字数、中文词数、英文词数、上屏次数；累计字数另折算为「约等于 n 本《某书》」，书目从《道德经》到《平凡的世界》。
这些只是若干数字，按天记录在本机，与输入日志无关：关闭或清空日志后统计仍在。
